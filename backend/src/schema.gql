# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ActiveSkill {
  cooldown_hours: Int!
  description: String!
  duration_hours: Int!
  icon: String!
  id: String!
  multiplier: Int!
  name: String!
}

type AssignPetResult {
  bonuses: PetBonuses!
  pet: Pet!
  playerHero: PlayerHero!
}

type AuthResponse {
  level: Int
  message: String
  playerId: String
  success: Boolean!
  token: String
  username: String
}

input BooleanFilter {
  equals: Boolean
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input DeployHeroInput {
  heroId: String!
  provinceId: Int!
}

type EraBenefits {
  expBonus: Int!
  goldBonus: Int!
  riceBonus: Int!
  stoneBonus: Int!
  unlockHeroes: [String!]!
  woodBonus: Int!
}

type EraInfo {
  benefits: EraBenefits!
  color: String!
  description: String!
  emoji: String!
  id: String!
  isCurrent: Boolean!
  isUnlocked: Boolean!
  landmarks: [String!]!
  maxStories: Int!
  minStories: Int!
  name: String!
  nameEnglish: String!
  progressPercentage: Float!
  remainingStories: Int!
  requiredStories: Int!
}

type EraProductionBonuses {
  experienceGain: Int!
  goldProduction: Int!
  riceProduction: Int!
  stoneProduction: Int!
  woodProduction: Int!
}

type EraTimeline {
  completedStories: Int!
  currentEraIndex: Int!
  playerId: String!
  timeline: [EraInfo!]!
}

type GrantExpResult {
  leveledUp: Boolean!
  levelsGained: Int!
  playerHero: PlayerHero!
}

type Hero {
  baseAttack: Int
  baseDefense: Int
  baseHp: Int
  baseSpeed: Int
  bonusType: String
  bonusValue: Int
  createdAt: DateTime!
  era: String
  id: ID!
  isAvailable: Boolean
  isPremium: Boolean
  nameEnglish: String
  nameVietnamese: String!
  petBonus: Int
  petEmoji: String
  petName: String
  rarity: String
  role: String
  storyDay: Int
  unlockRequirement: String
  updatedAt: DateTime!
}

type HeroStats {
  attack: Int!
  baseAttack: Int
  baseDefense: Int
  baseHP: Int
  baseSpeed: Int
  defense: Int!
  hp: Int!
  level: Int!
  speed: Int!
}

input HeroWhereInput {
  era: StringFilter
  id: StringFilter
  isAvailable: BooleanFilter
  isPremium: BooleanFilter
  nameVietnamese: StringFilter
  rarity: StringFilter
  role: StringFilter
  storyDay: IntFilter
}

input IntFilter {
  equals: Int
  gt: Int
  gte: Int
  in: [Int!]
  lt: Int
  lte: Int
  notIn: [Int!]
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

input LevelUpHeroInput {
  playerHeroId: String!
}

input MarkStoryReadInput {
  storyId: String!
}

type Mutation {
  addResources(bazan: Float = 0, gold: Float = 0, lumber: Float = 0, rice: Float = 0, stone: Float = 0): MutationResponse!
  assignPetToHero(heroId: String!, petId: String!): AssignPetResult!
  deployHero(input: DeployHeroInput!): PlayerHero!
  googleAuth(credential: String!): AuthResponse!
  grantExpToHero(expAmount: Int!, heroId: String!): GrantExpResult!
  grantExpToPet(expAmount: Int!, petId: String!): Pet!
  levelUpHero(input: LevelUpHeroInput!): PlayerHero!
  levelUpPet(petId: String!): Pet!
  login(email: String!, password: String!): AuthResponse!
  markStoryRead(input: MarkStoryReadInput!): String!
  recruitHero(input: RecruitHeroInput!): PlayerHero!
  register(email: String!, password: String!, username: String!): AuthResponse!
  resetPlayerData: MutationResponse!
  submitQuiz(input: SubmitQuizInput!): QuizSubmission!
  unassignPetFromHero(heroId: String!): Boolean!
  unlockProvince(input: UnlockProvinceInput!): PlayerProvince!
  updatePlayer(data: PlayerUpdateInput!): Player!
  upgradeProvince(input: UpgradeProvinceInput!): PlayerProvince!
  useProvinceSkill(provinceId: Int!): UseActiveSkillResult!
}

type MutationResponse {
  message: String
  success: Boolean!
}

input PaginationInput {
  skip: Int = 0
  take: Int = 10
}

type PassiveBuff {
  description: String!
  icon: String!
  source: String!
  type: String!
  value: Int!
}

type Pet {
  acquiredAt: DateTime!
  experience: Int
  id: ID!
  level: Int
  name: String!
  petType: String
  playerId: ID!
  rarity: String
}

type PetBonuses {
  allStats: Int
  attack: Int
  buildingSpeed: Int
  criticalChance: Int
  defense: Int
  description: String!
  expBonus: Int
  goldBonus: Int
  hp: Int
  icon: String!
  learningSpeed: Int
  luckBonus: Int
  productionSpeed: Int
  riceBonus: Int
  speed: Int
}

type PetWithBonuses {
  acquiredAt: DateTime!
  bonuses: PetBonuses!
  expForNextLevel: Int!
  expProgress: Int!
  experience: Int
  id: ID!
  level: Int
  name: String!
  petType: String
  playerId: ID!
  rarity: String
}

type Player {
  createdAt: DateTime
  email: String!
  experience: Int
  id: ID!
  lastLogin: DateTime
  level: Int
  premiumExpiresAt: DateTime
  premiumPassActive: Boolean
  region: String
  resources: JSON
  status: String
  updatedAt: DateTime
  username: String!
}

type PlayerCurrentEra {
  benefits: EraBenefits!
  completedStories: Int!
  currentEra: String!
  eraEmoji: String!
  eraName: String!
  isMaxEra: Boolean!
  playerId: String!
}

type PlayerEraBonuses {
  bonuses: EraProductionBonuses!
  description: String!
  era: String!
  eraName: String!
  playerId: String!
}

type PlayerHero {
  acquiredAt: DateTime!
  createdAt: DateTime!
  deployedTo: Int
  experience: Int
  hero: Hero!
  heroId: ID!
  id: ID!
  level: Int
  playerId: ID!
  updatedAt: DateTime!
}

input PlayerHeroWhereInput {
  deployedTo: IntFilter
  heroId: StringFilter
  level: IntFilter
  playerId: StringFilter
}

type PlayerHeroWithStats {
  acquiredAt: DateTime!
  createdAt: DateTime!
  deployedTo: Int
  expForNextLevel: Int!
  expProgress: Int!
  experience: Int
  hero: Hero!
  heroId: ID!
  id: ID!
  level: Int
  playerId: ID!
  stats: HeroStats!
  updatedAt: DateTime!
}

type PlayerProvince {
  buildingsCount: Int
  createdAt: DateTime!
  developmentLevel: Int
  farmerLevel: Int
  heroId: ID
  id: ID!
  playerId: ID!
  province: Province!
  provinceId: Int!
  resourceLevel: Int
  updatedAt: DateTime!
}

input PlayerProvinceWhereInput {
  development_level: IntFilter
  farmer_level: IntFilter
  player_id: StringFilter
  province_id: IntFilter
  resource_level: IntFilter
}

type PlayerProvinceWithSkills {
  activeSkill: ActiveSkill
  buildingsCount: Int
  createdAt: DateTime!
  developmentLevel: Int
  farmerLevel: Int
  heroId: ID
  id: ID!
  passiveBuffs: [PassiveBuff!]!
  playerId: ID!
  province: Province!
  provinceId: Int!
  resourceLevel: Int
  skillCooldown: SkillCooldownStatus
  updatedAt: DateTime!
}

type PlayerResource {
  amount: Int!
  createdAt: DateTime!
  id: ID!
  lastHarvestAt: DateTime
  playerId: ID!
  resourceType: String!
  updatedAt: DateTime!
}

type PlayerSynergies {
  activeSynergies: [ResourceSynergy!]!
  cycleCompletion: Float!
  playerId: String!
  totalBonusPercentage: Int!
  totalProvinces: Int!
}

input PlayerUpdateInput {
  email: String
  experience: Float
  level: Float
  username: String
}

input PlayerWhereInput {
  email: StringFilter
  id: StringFilter
  level: IntFilter
  premiumPassActive: BooleanFilter
  status: StringFilter
  username: StringFilter
}

type Province {
  baseBazanRate: Float
  baseGoldRate: Float
  baseRiceRate: Float
  baseStoneRate: Float
  baseWoodRate: Float
  createdAt: DateTime!
  description: String
  id: ID!
  isCapital: Boolean
  name: String!
  nameEnglish: String
  region: String
  unlockOrder: Int
  unlockStoryDay: Int
  updatedAt: DateTime!
}

type ProvinceData {
  baseBazanRate: Float
  baseGoldRate: Float
  baseRiceRate: Float
  baseStoneRate: Float
  baseWoodRate: Float
  description: String
  historicalEras: [String!]
  id: Int!
  isCapital: Boolean
  isOwned: Boolean!
  name: String!
  nameEnglish: String
  ownershipStatus: String!
  region: String!
  unlockOrder: Int
  unlockStoryDay: Int
}

type ProvinceDetails {
  baseBazanRate: Float
  baseGoldRate: Float
  baseRiceRate: Float
  baseStoneRate: Float
  baseWoodRate: Float
  description: String
  historicalEras: [String!]
  id: Int!
  isCapital: Boolean
  isOwned: Boolean!
  name: String!
  nameEnglish: String
  ownershipStatus: String!
  playerData: ProvincePlayerData
  productionRates: ProvinceProductionRates!
  region: String!
  stories: [StoryInfo!]!
  unlockOrder: Int
  unlockStoryDay: Int
}

type ProvincePlayerData {
  activeSkillLevel: Int!
  buildingsCount: Int!
  deployedHeroEra: String
  deployedHeroId: String
  deployedHeroName: String
  deployedHeroRarity: String
  developmentLevel: Int!
  farmerLevel: Int!
  passiveBuffs: [String!]!
  resourceLevel: Int!
}

type ProvinceProductionRates {
  bazan: Int!
  gold: Int!
  rice: Int!
  stone: Int!
  wood: Int!
}

type ProvinceSynergyInfo {
  applicableSynergies: [ResourceSynergy!]!
  hasSynergy: Boolean!
  primaryResources: [String!]!
  provinceId: Int!
  provinceName: String!
  totalBonus: Int!
}

type ProvinceUnlockInfo {
  isStartingProvince: Boolean!
  provinceId: Int!
  provinceName: String!
  requiredStory: StoryInfo
  requirementsDescription: String!
  unlockOrder: Int
  unlockStoryDay: Int
}

input ProvinceWhereInput {
  id: IntFilter
  is_capital: BooleanFilter
  name: StringFilter
  region: StringFilter
  unlock_order: IntFilter
  unlock_story_day: IntFilter
}

type Query {
  """Get all 63 provinces with ownership status"""
  allProvinces: [ProvinceData!]!
  availableStories: [StoryWithUnlockStatus!]!
  eraTimeline: EraTimeline!
  hero(id: String!): Hero!
  heroes(pagination: PaginationInput, where: HeroWhereInput): [Hero!]!
  me: Player
  myCurrentEra: PlayerCurrentEra!
  myEraBonuses: PlayerEraBonuses!
  myHero(heroId: String!): PlayerHero
  myHeroWithStats(heroId: String!): PlayerHeroWithStats
  myHeroes(where: PlayerHeroWhereInput): [PlayerHero!]!
  myHeroesWithStats: [PlayerHeroWithStats!]!
  myPet(petId: String!): Pet
  myPetWithBonuses(petId: String!): PetWithBonuses
  myPets: [Pet!]!
  myProvince(provinceId: Int!): PlayerProvince

  """Get player's owned province count"""
  myProvinceCount: Int!
  myProvinces(where: PlayerProvinceWhereInput): [PlayerProvince!]!
  myQuizSubmissions: [QuizSubmission!]!
  myResource(resourceType: String!): PlayerResource
  myResourceSynergies: PlayerSynergies!
  myResources: [PlayerResource!]!
  player(id: String!): Player
  players(pagination: PaginationInput, where: PlayerWhereInput): [Player!]!
  province(id: Int!): Province!

  """Get full details for a specific province"""
  provinceDetail(provinceId: Int!): ProvinceDetails!
  provinceSynergy(provinceId: Int!): ProvinceSynergyInfo!

  """Get unlock requirements for a province"""
  provinceUnlockInfo(provinceId: Int!): ProvinceUnlockInfo!
  provinceWithSkills(provinceId: Int!): PlayerProvinceWithSkills
  provinces(pagination: PaginationInput, where: ProvinceWhereInput): [Province!]!

  """Get provinces filtered by region"""
  provincesByRegion(region: String!): [ProvinceData!]!
  quizQuestions(storyId: String!): [QuizQuestion!]!

  """Get statistics for each region"""
  regionStatistics: RegionStatistics!
  resource(id: String!): Resource!
  resources: [Resource!]!

  """Search provinces by name or description"""
  searchProvinces(keyword: String!): [ProvinceData!]!
  stories(pagination: PaginationInput, where: StoryWhereInput): [Story!]!
  story(id: String!): Story!
  storyByDay(day: Int!): Story!

  """Get total number of provinces"""
  totalProvinceCount: Int!
  unlockableHeroes: UnlockableHeroesResult!
  wuXingCycle: WuXingCycleData!
}

input QuizAnswerInput {
  questionNumber: Int!
  selectedAnswer: Int!
}

type QuizQuestion {
  correctAnswer: Int!
  createdAt: DateTime!
  difficulty: String
  id: ID!
  options: JSON!
  question: String!
  questionNumber: Int!
  storyId: ID!
  type: String
}

type QuizSubmission {
  answers: JSON
  correctCount: Int
  id: ID!
  isPerfect: Boolean
  maxScore: Int!
  multiplier: Float
  perfectStreak: Int
  playerId: ID!
  rewards: JSON
  score: Int!
  storyId: ID!
  submittedAt: DateTime!
  timeTaken: Int
  totalQuestions: Int
}

input RecruitHeroInput {
  heroId: String!
}

type RegionStatistic {
  available: Int!
  locked: Int!
  owned: Int!
  total: Int!
}

type RegionStatistics {
  central: RegionStatistic!
  north: RegionStatistic!
  overall: RegionStatistic!
  south: RegionStatistic!
}

type Resource {
  baseGenerationRate: Float!
  baseStorageCapacity: Int!
  createdAt: DateTime!
  description: String
  elementType: String!
  emoji: String
  id: ID!
  nameEnglish: String!
  nameVietnamese: String!
  updatedAt: DateTime!
  uses: JSON
  valuePoints: Int
}

type ResourceSynergy {
  affectedProvinces: Int!
  bonusPercentage: Int!
  description: String!
  icon: String!
  sourceElement: String!
  sourceResource: String!
  targetElement: String!
  targetResource: String!
}

type SkillCooldownStatus {
  canUse: Boolean!
  isOnCooldown: Boolean!
  remainingHours: Int
  remainingSeconds: Int!
}

type Story {
  baseGoldReward: Int
  baseRiceReward: Int
  baseWoodReward: Int
  category: String!
  content: String!
  createdAt: DateTime!
  day: Int!
  era: String
  heroId: ID
  id: ID!
  isAvailable: Boolean
  isPremium: Boolean
  provinceId: Int
  readingTimeMinutes: Int
  titleEnglish: String!
  titleVietnamese: String!
  updatedAt: DateTime!
  wordCount: Int
}

type StoryInfo {
  day: Int!
  id: ID!
  isAvailable: Boolean!
  titleVietnamese: String!
}

input StoryWhereInput {
  category: StringFilter
  day: IntFilter
  era: StringFilter
  isAvailable: BooleanFilter
  isPremium: BooleanFilter
  provinceId: IntFilter
}

type StoryWithUnlockStatus {
  baseGoldReward: Int
  baseRiceReward: Int
  baseWoodReward: Int
  category: String!
  content: String!
  createdAt: DateTime!
  day: Int!
  daysSinceRegistration: Int!
  daysUntilUnlock: Int!
  era: String
  heroId: ID
  id: ID!
  isAvailable: Boolean
  isCompleted: Boolean!
  isPremium: Boolean
  isUnlocked: Boolean!
  provinceId: Int
  readingTimeMinutes: Int
  titleEnglish: String!
  titleVietnamese: String!
  updatedAt: DateTime!
  wordCount: Int
}

input StringFilter {
  contains: String
  endsWith: String
  equals: String
  gt: String
  gte: String
  in: [String!]
  lt: String
  lte: String
  notIn: [String!]
  startsWith: String
}

input SubmitQuizInput {
  answers: [QuizAnswerInput!]!
  storyId: String!
  timeTaken: Int
}

input UnlockProvinceInput {
  heroId: String
  provinceId: Int!
}

type UnlockableHeroInfo {
  era: String
  id: ID!
  name_vietnamese: String!
  rarity: String
}

type UnlockableHeroesResult {
  currentEra: String!
  playerId: String!
  totalUnlocked: Int!
  unlockableHeroes: [UnlockableHeroInfo!]!
}

input UpgradeProvinceInput {
  provinceId: Int!
  upgradeType: String!
}

type UseActiveSkillResult {
  cooldownEnds: DateTime!
  effectEnds: DateTime!
  playerProvince: PlayerProvince!
  skill: ActiveSkill!
}

type WuXingCycleData {
  activeSynergies: [ResourceSynergy!]!
  cycleCompletion: Float!
  cycleNodes: [WuXingNode!]!
  description: String!
  playerId: String!
  totalBonus: Int!
}

type WuXingNode {
  bonusPercentage: Int!
  element: String!
  elementName: String!
  emoji: String!
  isActive: Boolean!
  nextElement: String!
  nextResource: String!
  resource: String!
  resourceNameVN: String!
}